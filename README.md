# docker_multistage
This repository contains a Java application built using Maven. The project demonstrates containerization using Docker with a multi-stage build approach to optimize image size and performance.  Key Features:  Java application managed with Maven  Multi-stage Docker build for smaller, efficient images  Easy setup and deployment instructions
